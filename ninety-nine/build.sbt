name := "ninetynine"

version := "1.0"

scalaVersion := "2.10.2"

sourceDirectories in Compile +=  baseDirectory.value  / "src/main/scala/problems"

sourceDirectories in Compile +=  baseDirectory.value  / "src/main/scala/main"
